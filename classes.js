class Point{
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}

const DIR = {
  DOWN: 0,
  LEFT: 1,
  UP: 2,
  RIGHT: 3
}

const COLORS = {
  RED: "rgb(255, 0, 0)",
  BLUE: "rgb(0, 0, 255)"
}